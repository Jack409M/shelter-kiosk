from __future__ import annotations

"""
Legacy JotForm webhook module intentionally left blank.

DWC no longer uses the old JotForm compliance ingestion path, so this
module no longer exposes a blueprint or webhook route.
"""
